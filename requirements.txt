# Core data / ML stack
pandas==3.0.5
numpy==2.4.6
scikit-learn==1.9.0
imbalanced-learn==0.14.2
xgboost==3.2.0
joblib==1.5.3

# Visualization
matplotlib==3.11.1
seaborn==0.13.2

# Interactive analytics (dashboard/, docs/, outputs/interactive/)
plotly==6.9.0
dash==4.4.1

# Notebooks
jupyter>=1.0.0
ipykernel==7.3.0
nbconvert==7.17.1

# Testing
pytest==9.1.1

# Optional: SHAP-based feature importance (falls back to permutation
# importance automatically if this is not installed or fails to run).
# shap==0.46.0
